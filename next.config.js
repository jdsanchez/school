/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', '18.220.90.31', 'images.unsplash.com'],
  },
}

module.exports = nextConfig
